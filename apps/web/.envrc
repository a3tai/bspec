# BSpec Web (bspec.dev) Environment
source_up

# Cloudflare account ID inherited from root .envrc

# Clear wrangler cache to prevent wrong account issues
[[ -d "node_modules/.cache/wrangler" ]] && rm -rf "node_modules/.cache/wrangler"

echo "  📄 apps/web loaded (bspec.dev)"
